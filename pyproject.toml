[project]
name = "micropad"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kmk>=0.1.2",
]

[dependency-groups]
dev = [
    "circuitpython-stubs>=10.3.0",
]
